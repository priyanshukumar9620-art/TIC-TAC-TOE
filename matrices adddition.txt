#include <stdio.h>

int main()
{
    int m,n,q,l;
    printf("Enter the value of m and n for first materix:");
    scanf("%d%d",&m,&n);
    int a[m][n];
    printf("Enter the elements for first matrix:\n");
    for(int i=0;i<=m-1;i++){
        for(int j=0;j<=n-1;j++){
            scanf("%d",&a[i][j]);
        }
    }
    printf("Enter the value of m and n for second materix:");
    scanf("%d%d",&q,&l);
    if(n==q){
    int b[q][l];
    printf("Enter the elements for first matrix:\n");
    for(int i=0;i<=q-1;i++){
        for(int j=0;j<=l-1;j++){
            scanf("%d",&b[i][j]);
        }
    }
    int c[m][l];
    for(int i=0;i<=m-1;i++){
        for(int j=0;j<=l-1;j++){
            c[i][j]=0;
            for(int k=0;k<=n-1;k++){
                c[i][j]=c[i][j]+(a[i][k]*b[k][j]);
            }
            
        }
    }
    printf("The multiplication of matrix are\n");
    for(int i=0;i<=m-1;i++){
        for(int j=0;j<=l-1;j++){
            printf("%d ",c[i][j]);
        }printf("\n");
    }}else{
        printf("The number of columns first matrix and the number of rows of second matrix must qual");
    }
     
    

    return 0;
}